<script>
	history.pushState({}, '', '{{ "admin?entite=" ~ entite }}');
</script>

<section>
	<h1>{{ titre|upper }}</h1>
	<p>{{ messageErreur }}</p>
</section>

{{ include("modaleSuppression.twig") }}
{{ include("modaleSuppression.twig") }}
{{ include("modaleSuppression.twig") }}
{{ include("modaleSuppression.twig") }}
